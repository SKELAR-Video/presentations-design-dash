# Типи слайдів — закритий перелік

`правила: v12 · 14.08.2026`

Назви — ті самі, що в Figma-бібліотеці типів, щоб ми, ви й генератор говорили одними словами. **Типу немає в цьому файлі — його не існує.** Контент, який не лягає в жоден тип, — привід спитати людину, а не вигадати композицію.

Значення нижче взяті з файлу через `get_design_context`, а не зняті на око.

## Спільне для всіх типів

| | значення |
|---|---|
| слайд | `1920×1080`, фон `#090D17` |
| поле | `100` з усіх боків; ширина контенту `1720`; низ контенту `980` |
| шрифт | Inter, вага `500` завжди; інтерліньяж тексту `1.18`, заголовка `1` |
| картка | фон `#292D39`, радіус `30`, внутрішній відступ `40` |
| текст картки | `#A2A6B1`; білий лишається для номерів і заголовків |
| знак квадратний | `90×90` — правий верх `1730,100` або лівий низ `100,890` |
| знак довгий | `357.5×90` на `1463,100` — тільки на перебивці |
| акцент | `#FD3433`: плашка знака, шкала адженди, точка, кружечок із номером |

**Кегль — не константа.** Заголовок `118` там, де вміщається; `87` там, де колонка вужча (тип із фото або з лінією). Береться найбільший, при якому нічого не переповнено й не лишилось невикористаного простору. Те саме для тексту карток: `48` при короткому, `38` при довгому.

## Перелік типів

| Тип | Коли брати | Ємність | Слайд у `reference-basic.html` |
|---|---|---|---|
| `Title+txt` | перший слайд дека, розділ | 1 теза `157` + підзаголовок `48` до ~140 знаків | 1 |
| `Title` червона | головний розділ | 1 теза, кегль від довжини | 2 |
| `Title` з фото | обкладинка дека | 1 теза + дата-чип | 14 |
| `Agenda N` | план виступу | 3–8 пунктів. Сітка **однакова в усіх рядках**: 5–8 → `ceil(N/2)`, максимум 4, остання клітинка порожня. Два рядки кріпляться до лінії 980 і ростуть угору | 3, 4, 5 |
| `Bento Bottom N` | 2–4 тези в нижній смузі | 2, 3 або 4; смуга `500→980` | 6, 7 |
| `Bento Bottom Number N` | те саме, але з номерами `01…` | 2, 3 або 4; номер `98` | 8 |
| `Bento Right N` | довгий заголовок ліворуч, картки праворуч | 1–3; колонка `860` на `x960`, знак у лівому низу | 9 |
| `Column Red Circle N` | 2–4 тези з нумерацією без карток | 2, 3 або 4; кружечок `75`, цифра `48` | 10 |
| `Column Text N` | надзаголовок і абзац, без цифр і карток | 2, 3 або 4; надзаголовок `48` сірий, тіло `38` біле | 11 |
| `Column Red Line N` | те саме, але з вертикальною лінією | 2 або 3; текст відступає на `85` — там точка з лінією | 12 |
| `Half screen Bullits N` | теза з образом | 2–4 пункти; фото рівно права половина `960×1080`, `cover` | 13 |

Окремого типу «обкладинка» в бібліотеці немає: перший слайд дека — це `Title` з фоном-фото.

## Слайди з даними — `reference.html`

| Тип | Коли брати | Ємність | Слайд |
|---|---|---|---|
| Щільна таблиця | порівняння за багатьма ознаками | до 8 рядків × 8 колонок | 1 |
| Рейтинг зі смугами | впорядкований список із величиною | 3 колонки × 10 рядків | 2 |
| Дві таблиці поруч + KPI | два зрізи одного явища | 7 і 10 рядків | 3 |
| Сітка KPI | окремі числа без спільної шкали | 8 карток | 4 |
| Графік-рейтинг + твердження | 10 позицій і висновки з них | 10 рядків + 5 тверджень | 5 |

Ця група прийшла з дашборд-еталона й має власну палітру (фон `#0F131D`), бо її погоджували окремо. Не змішувати з базовими типами в одному деку без потреби.

## Що робити, коли тип не підходить

1. Перевірити, чи це не інший тип: пʼять пунктів у адженді — це адженда на два рядки, а не нова сітка.
2. Перевірити, чи контент не ділиться на два слайди того ж типу.
3. Не підходить — **сказати людині**: «на N елементів у типі X дизайну немає, ділимо на два слайди або потрібен новий макет». І чекати відповіді.

Виняток, узгоджений окремо: **адженда** понад 8 пунктів розставляється щільніше в тому ж патерні, новий тип не вводиться.

Не робити: не тиснути кегль нижче того, що читається, не звужувати поля, не ліпити ще одну картку, не переносити зайве в нотатки доповідача.

## Логотип

**На червоному тлі — тільки білий знак:** `SKELAR Logo white.png` (довгий) або `SKELAR Symbol for red.png` (квадратний). Червона плашка на червоному не читається.

Позицій дві, третьої немає: правий верх, а коли він зайнятий контентом — лівий низ. У кожній навколо знака ≥50px порожнечі, і міряється це до краю картки та до намальованого тексту. Не влазить у жодну — знак не ставиться взагалі. Зменшених версій не буває.

## Червоний

Тільки на цих поверхнях: знак і плашка логотипа, шкала адженди та її точки, вертикальна лінія та її точка, кружечок із номером, смуга в графіку зі значенням «наше».

**Текст червоним не буває ніколи** — ні слово, ні число, ні заголовок. Виділення робиться розміром.
